# BarberKas AaaS — local dev secrets template
# Copy to .dev.vars (gitignored) and fill. For prod use `wrangler secret put`.
GROQ_API_KEY=
OPENROUTER_API_KEY=
FONNTE_TOKEN=
# Shared secret utk verifikasi webhook Fonnte (?secret= di URL webhook) — generate: openssl rand -hex 24
FONNTE_WEBHOOK_SECRET=
# BKF-20: mode paket FREE Fonnte — '1' = selalu soft-sanitize kiriman WA (bullet/emoji-berat dilunakkan),
# '2' = selalu plain-text. Kosong/'0' = kirim format asli dulu; sanitize otomatis hanya saat Fonnte menolak.
FONNTE_FREE_SANITIZE=
WABLAS_TOKEN=
# Duitku Pop (MoR rel uang via Oasis BI Pro)
DUITKU_MERCHANT_CODE=DXXXXX
DUITKU_MERCHANT_KEY=your-duitku-api-key
DUITKU_ENV=sandbox
JWT_SECRET=change-me-to-a-long-random-string
# Clerk auth (BKF-14/15) — prod pakai `wrangler pages secret put`
CLERK_SECRET_KEY=sk_live_or_sk_test_xxx
CLERK_PUBLISHABLE_KEY=pk_live_or_pk_test_xxx
CLERK_ISSUER=https://clerk.barberkas-foundry.biz.id
# DEV_AUTH_BYPASS_EMAIL=owner@cutoclock.test  # HANYA lokal E2E — JANGAN di prod
